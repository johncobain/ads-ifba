#include <stdio.h>

int main(){
    int operando1;
    int operando2;
    int soma;

    scanf("%d",&operando1);
    scanf("%d",&operando2);
    soma = operando1 + operando2;

    printf("%d",soma);

    return 0;
}