events { worker_connections 1024; }

http {
    upstream servidor_backend {
        server app1:3000;
        server app2:3000;
        server app3:3000;
    }

    server {
        listen 80;
        location / {
            proxy_pass http://servidor_backend;
        }
    }
}
